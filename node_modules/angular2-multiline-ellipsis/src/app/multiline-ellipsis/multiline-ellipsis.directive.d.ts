import { AfterViewInit, ElementRef } from '@angular/core';
/**
 * Removes excess text from element until it fits in elements
 * and appends a ellipsis symbol to end of text. This requires that
 * the elements height be fixed and it's `overflow` css property
 * be `hidden`
 *
 * @example
 * ```html
 * <p appEllipsis>Lorem ipsum dolor</p>
 * ```
 *
 */
export declare class MultilineEllipsisDirective implements AfterViewInit {
    private el;
    private platformId;
    /**
     * Ellipsis charater
     */
    private dotChar;
    /**
     * init text when load page
     */
    private textInit;
    /**
     * contructor
     */
    constructor(el: ElementRef, platformId: any);
    /**
     * after view init
     */
    ngAfterViewInit(): void;
    /**
     * ellipsis Function
     */
    private ellipsisFunc();
    /**
     * listen window resize
     */
    onResize(event: any): void;
}
